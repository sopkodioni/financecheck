import baseStyles from '../base.module.scss'
import headerStyles from './HeaderNav.module.scss'

export const HeaderNav = () => {
    return (
        <div className={`${baseStyles.dashboardBox} ${headerStyles.header}`}>
            
        </div>
    )
}